" Use the default C-p for ctrlp so C-f still moves a page forward.
let g:ctrlp_map = '<C-p>'

let g:lsc_server_commands = {'dart': 'dart_language_server'}

source /usr/share/doc/fzf/examples/fzf.vim

map g<C-p> :Commands<cr>

set number

